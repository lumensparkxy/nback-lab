# Agree the fixed-level visual n-back session rules

Type: specification. Priority: high. Ready for discussion, not gameplay implementation.

Specification: `docs/features/F001-visual-session.md`.

Resolve the open questions with the owner. Provide at least one fixed example
sequence with expected eligible matches and response outcomes. Define exact timing
boundaries, score calculation and lifecycle behavior. Complete acceptance criteria
and verification mapping; record owner agreement and set the spec to Agreed.

Exclude application/engine implementation. Unblocks I001.
